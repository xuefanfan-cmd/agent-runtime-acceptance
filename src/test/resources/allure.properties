# Allure JUnit 5 listener output directory.
# Without this, allure-jupiter may fail to detect the Maven build directory and
# fall back to ./allure-results (CWD) instead of the standard target/allure-results.
allure.results.directory=target/allure-results
