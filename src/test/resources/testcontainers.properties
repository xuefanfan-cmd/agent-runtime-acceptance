# Testcontainers image resolution for environments where Docker Hub is slow or blocked.
# Local images tagged as redis:7-alpine / testcontainers/ryuk:0.14.0 are used when present.
hub.image.name.prefix=docker.1ms.run/library/
ryuk.container.image=docker.1ms.run/testcontainers/ryuk:0.14.0
