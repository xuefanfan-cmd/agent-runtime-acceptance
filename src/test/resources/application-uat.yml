# UAT environment configuration
# UAT timeout overrides (remote/pre-deployed SUT).

sut:
  timeout:
    poll-seconds: 60
    poll-interval-ms: 2000
    connect-timeout-seconds: 15
    read-timeout-seconds: 60
